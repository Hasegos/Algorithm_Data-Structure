1.참조 자료형(Reference type)과 기본 자료형(Primitive type)의 차이를 정리해 주세요.

    내 답변 : 참조 자료형은 new 예약어를 사용해서 생성자 생성과 동시에 객체를 생성해야하고 기본자료형은 그럴필요가없다
    
    
2.기본 자료형 8 가지를 나열하고 각 타입의 특징을 정리해 주세요.

    내 답변 : int : 정수형 , char : 정수형 , byte : 정수형 , long : 정수형
            float : 실수형 , double : 실수형인데 float보다 더 큼 
            String : 문자열 , boolean : 참과 거짓
            
            
3. 형 변환이란 무엇이고 왜 해야 하나요?

    내 답변 : casting 해야되는 이유는 각 타입에 맞게끔 정보를 넣기위해서
    
    
4.if 문의 용도는 무엇이며, if-else와 if-else if 는 어떤 점이 다른지 정리해 주세요.

    내 답변 : 조건문이고 if-else는 if 문이 아니면 무조건 else 이고 if-else 는 if문말고 그다음 조건을 확인한다.
    
    
5.switch-case 문의 용도를 정리해 주세요.

    내 답변 : 조건마다실행하기위해
    
    
6.for, do-while, while 를 어떻게 사용하는지 1부터 10까지 더하는 코드를 예를 들어 정리해 주세요.

    내 답변 : for(int i = 1; i < 11; i++){    
             }
             int i = 1; do{ i++; } while(i < 11);
             int i = 1; while(i < 11) { i++; }
             
             
7.학생이라면 지금까지의 자신의 학점이나 등수를, 회사원이라면 지금까지의 고과를 String 배열에 넣고 출력하는 코드를 작성해 주세요.

    내 답변 : String[] garde = new String []{
        // 학점들
    }
    
    
8.생성자는 무엇을 하는데 사용하는 것이며, 별도로 만들지 않아도 자동으로 생성되는 생성자에 대해서 정리해 주세요.
    
    내 답변 : 생성자는 그 클래스를 호출시 발생되는 행위를 말하며, 자동으로 생성되는 생성자는 아무런 내용이 없다.
    
    
9.Overloading은 무엇인가요? public void setData(int a) 라는 메소드를 원하시는 대로 Overloading 해 주세요.

    내 답변 : Overloading은 같은 이름의 메소드를 매개변수(메세지) 타입, 갯수를 다르게해서 재사용하는 것을 말한다.
             예시)public void setData(int a , int b)
             
             
10.패키지를 선언하는 위치와 이름을 지정할 때의 유의점을 정리해 주세요.

    내 답변 : 파일의 가장 첫줄에 사용해야하며 import를 사용한다  이름은 java로 시작해서는 안된다.


11.다른 패키지에 선언된 클래스를 사용하기 위한 import 는 어디 위치에 선언해야하며, static import 은 무엇인지 정리해 주세요.

    내 답변 : 가장 첫줄에 사용해야되며, static import는 static 변수와 메소드들만 사용할 수 있게 선언한다.
    
    
12.클래스란 무엇인가요? 다음의 단어들이 포함되는 문장을 작성하고, 주어진 단어의 의미도 같이 정리해 주세요.(속성, 상태)

    내 답변 : 클래스란 메소드나 변수 등 또는 행위와 상태를 포함하는것을 클래스라한다. 
    속성 : ? 상태 : 클래스나 인스턴스 변수를 말한다.
   
   
13.인터페이스, abstract 클래스, 클래스, Enum 클래스가 있는데 각각의 특징 및 다른 점을 정리해 주세요.

    내 답변 : 인터페이스 : 상속받은 클래스는 무조건 인터페이스에 있는것을 구현해야되며, 인터페이스에있는 변수는 final이다.
                        여러개 상속을 받을 수 있으며, implements를 사용한다. 인스턴스화가 불가능핟. 메소드 구현이 불가능 선언만가능.
             abstract 클래스 : 클래스 앞에 abstract 예약어를 붙이며 상속을 받을땐 extends로 한다.
                              또한, 추상클래스에 있는 메소드 앞에 abstract가 있으면 무조건 상속받은 클래스에서 구현해야되고, 클래스내에서 구현이 가능하다.
                              인스턴스화 불가능하다.
             클래스 : 메소드와 변수가 포함된다. 클래스 상속은 하나만 가능하다. 인스턴스화 가능하며, 클래스 변수 사용이가능하다.              
             Enum 클래스 : 상수들의 집합 클래스이며, 생성자를 통해 상수를 클래스내에 정할 수 있다.
                          또한 Enum클래스는 메소드 구현이 가능하다. 상속이 불가능하다.    
                          
                          
14.instanceof 라는 연산자의 용도를 정리해 주세요.

    내 답변 : instanceof는 해당 객체가 무슨타입인지를 확인할수있다.


15.어떤 클래스를 상속받아 확장을 하면, 부모 클래스의 어떤 것들을 사용할 수 있는지 정리해 주세요.

    내 답변 : 부모클래스 내에 존재하는 메소드를 Override를 사용해서 재사용이 가능하다.
             추가로 부모클래스에 존재하는 변수(super 예약어) 또한 사용이 가능하다.
             
             
16.변수를 final 로 선언하는 것이 어떤 의미가 있는지 정리해 주세요.

    내 답변 : 변수를 final로 선언시 이는 상수를 뜻하고 
             직접 그 final로 선언된 변수를 손을 대지않는한 변수의 값을 바꿀수 없으며, 참조만가능하다.


17.클래스를 final 로 선언하는 것이 어떤 의미가 있는지 정리해 주세요.

    내 답변 : 클래스를 final로 선언시 더이상 상속이 불가능하다.
             
18.변수를 static 으로 선언하는 것이 어떤 의미가 있는지 정리해 주세요.
    
    내 답변 : 변수가 static으로 선언되면 클래스 기반의 변수라는 의미이다.
             인스턴스 변수에서 static 기반 변수를 사용하면안되고 클래스 자체 변수로 사용해야한다.
             
             
19.메소드를 static 으로 선언하는 것이 어떤 의미가 있는지 정리해 주세요.

    내 답변 : 클래스 기반 메소드 임으로 인스턴스화를 못시킨다.
             클래스 기반.메소드 형식으로 사용해야한다.
             
             
20.try-catch-finally 블록은 왜 사용하고 각각의 블록이 어떤용도로 사용되는지 정리해 주세요.
    
    내 답변 : try - catch {} 는 try 구문에 있는 예외가 발생이 높은 내용을 실행한후 예외가 발생하면 catch에서 해당예외 구문에서 잡아서 예외처리를 해준다.
             finally 는 예외와 상관없이 무조건적으로 실행한다.
             
             
21.String 클래스와 StringBuilder, StringBuffer 클래스의 차이를 정리해 주세요.

    내 답변 : String 클래스는 자바에서 문자열을 다루는 클래스이며, String클래스에서 정의된 애들을 사용할수있다.
             StringBuilder 와 StringBuffer이 나온이유는 문자열 처리를 하다보면 String에서 문자열 생성과정에있다.
             기존에 있는 문자열을 버리고 새로운 문자열을 만드는데 이때 GC(가비지 컬렉션)이 발생함으로 메모리를 많잊 잡아먹는 사항이 있어서
             나왔으며 StringBuilder와 StringBuffer 차이는 전자는 쓰레드에서 안전하지않고 후자는 쓰레드에서 안전하다.
             
             
22.String 클래스의 indexOf(), substring(), split(), getBytes() 메소드가 어떤 용도로 사용되는지 정리해 주세요.

    내 답변 : indexOf()는 해당 위치 또는 문자를 리턴해준다.
             substring() 해당 위치부터 문자열을 자른다.
             getBytes() 문자열을 바이트 값으로 변환해준다.
