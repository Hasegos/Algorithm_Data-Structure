1.BigDecimal 클래스의 용도가 무엇인지 확인해보자.

    내 답변 : 숫자를 정밀하게 저장하고 표현할 수 있는 용도
    
    
2.BigDecimal 클래스의 생성자가 몇개인지 확인해보자

    내 답변 : 16개
    
    
3.BigDecimal 클래스에 있는 메소드 중 abs() 메소드의 용도는 무엇인가

    내 답변 : BigDecimal 절대값이고 소수자리가 this.scale()을  BigDecimal 로 돌려준다
    
    
4.BigDecimal 클래스에 있는 메소드 중 현재값에서 특정 값을 뺴는 연산을 수행하는 메소드는 어떤것인가?

    내 답변 : substract()
    
    
5.BigDecimal 클래스에 있는 메소드 중 나누기와 관련된 메소드에는 어떤 것들이 있는지 확인해보자.

    내 답변 : divide() , divideAndRemainder() , divideToIntegralValue()
    
    
